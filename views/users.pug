block content
  h1 #{title}
    h1 WELCOME BACK #{user.name}
    a(href="/logout") LOG OUT
    br
    p Your day count of none Fap : #{user.fapCount}
    
if errors 
    ul
      for error in errors
        li!= error.msg
